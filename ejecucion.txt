MAC:

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install curl_cffi
pip install ib_insync
opcion 1 (dashboard, requiere TWS/IB Gateway abierto):
streamlit run dashboard_ibkr.py

opcion 2 (CLI suelto, solo Elliott/Fib, sin IBKR):
 python3 elliott_trader_pro.py --ticker WYFI --period 1y --deviation 3.0


WIN:

python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
pip install curl_cffi
pip install ib_insync

streamlit run dashboard_ibkr.py